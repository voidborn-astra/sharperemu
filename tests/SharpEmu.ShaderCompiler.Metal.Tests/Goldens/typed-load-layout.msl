// Generated by SharpEmu Gen5MslTranslator.
#include <metal_stdlib>

using namespace metal;

static constant uint sharpemu_gfx10_formats[128] = {
    0x0u, 0x1u, 0x101u, 0x201u, 0x301u, 0x401u, 0x501u, 0x2u, 0x102u, 0x202u, 0x302u, 0x402u, 0x502u, 0x702u, 0x3u, 0x103u,
    0x203u, 0x303u, 0x403u, 0x503u, 0x404u, 0x504u, 0x704u, 0x5u, 0x105u, 0x205u, 0x305u, 0x405u, 0x505u, 0x705u, 0x0u, 0x0u,
    0x0u, 0x0u, 0x0u, 0x0u, 0x706u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x707u, 0x8u, 0x108u, 0x0u, 0x0u,
    0x408u, 0x508u, 0x9u, 0x109u, 0x209u, 0x309u, 0x409u, 0x509u, 0xAu, 0x10Au, 0x20Au, 0x30Au, 0x40Au, 0x50Au, 0x40Bu, 0x50Bu,
    0x70Bu, 0xCu, 0x10Cu, 0x20Cu, 0x30Cu, 0x40Cu, 0x50Cu, 0x70Cu, 0x40Du, 0x50Du, 0x70Du, 0x40Eu, 0x50Eu, 0x70Eu, 0x0u, 0x0u,
    0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u,
    0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u,
    0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u, 0x0u,
};

static inline void sharpemu_format_layout(uint dfmt, uint component, thread uint& byteOff, thread uint& bitOff, thread uint& bits)
{
    byteOff = 0u; bitOff = 0u; bits = 0u;
    switch (component * 16u + dfmt)
    {
    case 1u: byteOff = 0u; bitOff = 0u; bits = 8u; break;
    case 2u: byteOff = 0u; bitOff = 0u; bits = 16u; break;
    case 3u: byteOff = 0u; bitOff = 0u; bits = 8u; break;
    case 4u: byteOff = 0u; bitOff = 0u; bits = 32u; break;
    case 5u: byteOff = 0u; bitOff = 0u; bits = 16u; break;
    case 6u: byteOff = 0u; bitOff = 0u; bits = 10u; break;
    case 7u: byteOff = 0u; bitOff = 0u; bits = 11u; break;
    case 8u: byteOff = 0u; bitOff = 0u; bits = 10u; break;
    case 9u: byteOff = 0u; bitOff = 0u; bits = 2u; break;
    case 10u: byteOff = 0u; bitOff = 0u; bits = 8u; break;
    case 11u: byteOff = 0u; bitOff = 0u; bits = 32u; break;
    case 12u: byteOff = 0u; bitOff = 0u; bits = 16u; break;
    case 13u: byteOff = 0u; bitOff = 0u; bits = 32u; break;
    case 14u: byteOff = 0u; bitOff = 0u; bits = 32u; break;
    case 19u: byteOff = 1u; bitOff = 0u; bits = 8u; break;
    case 21u: byteOff = 2u; bitOff = 0u; bits = 16u; break;
    case 22u: byteOff = 0u; bitOff = 10u; bits = 11u; break;
    case 23u: byteOff = 0u; bitOff = 11u; bits = 11u; break;
    case 24u: byteOff = 0u; bitOff = 10u; bits = 10u; break;
    case 25u: byteOff = 0u; bitOff = 2u; bits = 10u; break;
    case 26u: byteOff = 1u; bitOff = 0u; bits = 8u; break;
    case 27u: byteOff = 4u; bitOff = 0u; bits = 32u; break;
    case 28u: byteOff = 2u; bitOff = 0u; bits = 16u; break;
    case 29u: byteOff = 4u; bitOff = 0u; bits = 32u; break;
    case 30u: byteOff = 4u; bitOff = 0u; bits = 32u; break;
    case 38u: byteOff = 0u; bitOff = 21u; bits = 11u; break;
    case 39u: byteOff = 0u; bitOff = 22u; bits = 10u; break;
    case 40u: byteOff = 0u; bitOff = 20u; bits = 10u; break;
    case 41u: byteOff = 0u; bitOff = 12u; bits = 10u; break;
    case 42u: byteOff = 2u; bitOff = 0u; bits = 8u; break;
    case 44u: byteOff = 4u; bitOff = 0u; bits = 16u; break;
    case 45u: byteOff = 8u; bitOff = 0u; bits = 32u; break;
    case 46u: byteOff = 8u; bitOff = 0u; bits = 32u; break;
    case 56u: byteOff = 0u; bitOff = 30u; bits = 2u; break;
    case 57u: byteOff = 0u; bitOff = 22u; bits = 10u; break;
    case 58u: byteOff = 3u; bitOff = 0u; bits = 8u; break;
    case 60u: byteOff = 6u; bitOff = 0u; bits = 16u; break;
    case 62u: byteOff = 12u; bitOff = 0u; bits = 32u; break;
    default: break;
    }
}

static inline uint sharpemu_minifloat(uint raw, uint bits)
{
    uint mantissaBits = bits - 5u;
    uint mantissa = raw & ((1u << mantissaBits) - 1u);
    uint exponent = (raw >> mantissaBits) & 0x1Fu;
    uint shift = 23u - mantissaBits;
    if (exponent == 31u)
    {
        return 0x7F800000u | (mantissa << shift);
    }
    if (exponent == 0u)
    {
        float scale = mantissaBits == 6u ? (1.0f / 1048576.0f) : (1.0f / 524288.0f);
        return as_type<uint>((float)mantissa * scale);
    }
    return ((exponent + 112u) << 23) | (mantissa << shift);
}

static inline uint sharpemu_format_one(uint nfmt)
{
    return (nfmt == 4u || nfmt == 5u) ? 1u : 0x3F800000u;
}

static inline uint sharpemu_format_convert(uint raw, uint bits, uint nfmt, uint dfmt)
{
    uint lowMask = bits >= 32u ? 0xFFFFFFFFu : ((1u << bits) - 1u);
    int signedRaw = extract_bits(as_type<int>(raw), 0u, bits);
    switch (nfmt)
    {
    case 0u: return as_type<uint>((float)raw / (float)lowMask);
    case 1u:
    {
        float snorm = (float)signedRaw / (float)(lowMask >> 1);
        return as_type<uint>(fmax(snorm, -1.0f));
    }
    case 2u: return as_type<uint>((float)raw);
    case 3u: return as_type<uint>((float)signedRaw);
    case 5u: return (uint)signedRaw;
    case 7u:
    {
        // 10_11_11/11_11_10 packed floats are unsigned mini-floats.
        if (dfmt == 6u || dfmt == 7u)
        {
            return sharpemu_minifloat(raw, bits);
        }
        if (bits == 16u)
        {
            return as_type<uint>((float)as_type<half>((ushort)(raw & 0xFFFFu)));
        }
        return raw;
    }
    default: return raw;
    }
}

static inline uint sharpemu_minifloat_encode(uint value, uint bits)
{
    uint mantissaBits = bits - 5u;
    uint shift = 23u - mantissaBits;
    float f = as_type<float>(value);
    if (isnan(f))
    {
        return (1u << bits) - 1u;
    }
    if (!(f > 0.0f))
    {
        return 0u;
    }
    uint exponent = (value >> 23) & 0xFFu;
    if (exponent < 113u)
    {
        // Below 2^-14 the result is a denormal: the mantissa alone scales the value.
        return (uint)rint(f * (float)(1u << (14u + mantissaBits)));
    }
    // Rounding the dropped mantissa bits may carry into the exponent.
    uint rounded = value + ((1u << (shift - 1u)) - 1u) + ((value >> shift) & 1u);
    uint roundedExponent = (rounded >> 23) & 0xFFu;
    if (roundedExponent >= 143u)
    {
        return 31u << mantissaBits;
    }
    return ((roundedExponent - 112u) << mantissaBits) | ((rounded >> shift) & ((1u << mantissaBits) - 1u));
}

static inline uint sharpemu_format_encode(uint value, uint bits, uint nfmt, uint dfmt)
{
    if (bits >= 32u)
    {
        return value;
    }
    uint mask = (1u << bits) - 1u;
    uint signedMaximum = mask >> 1u;
    float f = as_type<float>(value);
    // NaN stores as zero in the normalized and scaled formats.
    float finite = isnan(f) ? 0.0f : f;
    switch (nfmt)
    {
    case 0u: return (uint)rint(clamp(finite, 0.0f, 1.0f) * (float)mask);
    case 1u: return (uint)(int)rint(clamp(finite, -1.0f, 1.0f) * (float)signedMaximum) & mask;
    case 2u: return (uint)rint(clamp(finite, 0.0f, (float)mask));
    case 3u: return (uint)(int)rint(clamp(finite, -(float)signedMaximum - 1.0f, (float)signedMaximum)) & mask;
    case 4u: return min(value, mask);
    case 5u: return (uint)clamp((int)value, -(int)signedMaximum - 1, (int)signedMaximum) & mask;
    case 7u:
    {
        if (dfmt == 6u || dfmt == 7u)
        {
            return sharpemu_minifloat_encode(value, bits);
        }
        if (bits == 16u)
        {
            return (uint)as_type<ushort>((half)f);
        }
        return value & mask;
    }
    default: return value & mask;
    }
}

struct Gen5AddressRange
{
    ulong first_page;
    ulong page_count;
    device uint* base;
};

// Finds the mapped range of a guest address; a miss records the page in the fault bitmap.
static inline device uint* sharpemu_resolve_device_address(device const Gen5AddressRange* ranges, uint range_count, device uint* faults, uint fault_words, ulong address, thread bool& valid)
{
    ulong masked = address & 0xFFFFFFFFFFFFul;
    ulong page = masked >> 14u;
    uint low = 0u;
    uint high = range_count;
    for (uint step = 0u; step < 7u; step++)
    {
        uint middle = low + ((high - low) >> 1u);
        bool move_up = low < high && ranges[middle].first_page <= page;
        low = move_up ? middle + 1u : low;
        high = move_up ? high : middle;
    }
    valid = false;
    if (low != 0u)
    {
        Gen5AddressRange range = ranges[low - 1u];
        if (page - range.first_page < range.page_count)
        {
            valid = true;
            ulong byte_offset = ((page - range.first_page) << 14u) + (masked & 0x3FFFul);
            return (device uint*)((device uchar*)range.base + byte_offset);
        }
    }
    uint page_index = (uint)page;
    uint word = page_index >> 5u;
    if (word < fault_words)
    {
        atomic_fetch_or_explicit((device atomic_uint*)(faults + word), 1u << (page_index & 31u), memory_order_relaxed);
    }
    return nullptr;
}

static inline uint sharpemu_load_device_dword(device const Gen5AddressRange* ranges, uint range_count, device uint* faults, uint fault_words, ulong address)
{
    bool valid = false;
    device uint* word = sharpemu_resolve_device_address(ranges, range_count, faults, fault_words, address, valid);
    return valid ? *word : 0u;
}

// An unaligned value assembled from the two aligned dwords it spans.
static inline uint sharpemu_load_device_unaligned(device const Gen5AddressRange* ranges, uint range_count, device uint* faults, uint fault_words, ulong address, uint count)
{
    uint alignment = (uint)(address & 3ul);
    ulong aligned = address & ~3ul;
    uint shift = alignment * 8u;
    uint low = sharpemu_load_device_dword(ranges, range_count, faults, fault_words, aligned);
    uint high = (alignment + count > 4u) ? sharpemu_load_device_dword(ranges, range_count, faults, fault_words, aligned + 4ul) : 0u;
    return (low >> shift) | (shift == 0u ? 0u : (high << (32u - shift)));
}

static inline void sharpemu_store_device_dword(device const Gen5AddressRange* ranges, uint range_count, device uint* faults, uint fault_words, ulong address, uint value)
{
    bool valid = false;
    device uint* word = sharpemu_resolve_device_address(ranges, range_count, faults, fault_words, address, valid);
    if (valid)
    {
        *word = value;
    }
}

static inline void sharpemu_store_device_masked_word(device const Gen5AddressRange* ranges, uint range_count, device uint* faults, uint fault_words, ulong address, uint value, uint mask)
{
    if (mask == 0u)
    {
        return;
    }
    bool valid = false;
    device uint* word = sharpemu_resolve_device_address(ranges, range_count, faults, fault_words, address, valid);
    if (!valid)
    {
        return;
    }
    if (mask == 0xFFFFFFFFu)
    {
        *word = value;
        return;
    }
    device atomic_uint* atomic_word = (device atomic_uint*)word;
    uint observed = atomic_load_explicit(atomic_word, memory_order_relaxed);
    while (!atomic_compare_exchange_weak_explicit(atomic_word, &observed, (observed & ~mask) | value, memory_order_relaxed, memory_order_relaxed))
    {
    }
}

// A sub-dword store merges into the dwords it touches.
static inline void sharpemu_store_device_bytes(device const Gen5AddressRange* ranges, uint range_count, device uint* faults, uint fault_words, ulong address, uint value, uint count)
{
    uint element_mask = count == 1u ? 0xFFu : 0xFFFFu;
    uint alignment = (uint)(address & 3ul);
    ulong aligned = address & ~3ul;
    uint shift = alignment * 8u;
    uint carry_shift = 32u - shift;
    bool crosses = alignment + count > 4u;
    uint masked = value & element_mask;
    sharpemu_store_device_masked_word(ranges, range_count, faults, fault_words, aligned, masked << shift, element_mask << shift);
    sharpemu_store_device_masked_word(ranges, range_count, faults, fault_words, aligned + 4ul, crosses ? (masked >> carry_shift) : 0u, crosses ? (element_mask >> carry_shift) : 0u);
}

static inline uint sharpemu_atomic_step(device atomic_uint* word, bool increment)
{
    uint observed = atomic_load_explicit(word, memory_order_relaxed);
    while (!atomic_compare_exchange_weak_explicit(word, &observed, increment ? observed + 1u : observed - 1u, memory_order_relaxed, memory_order_relaxed))
    {
    }
    return observed;
}

// Compare-and-swap that retries only spurious failures; returns the value observed.
static inline uint sharpemu_atomic_compare_swap(device atomic_uint* word, uint compare, uint value)
{
    uint expected = compare;
    while (!atomic_compare_exchange_weak_explicit(word, &expected, value, memory_order_relaxed, memory_order_relaxed) && expected == compare)
    {
    }
    return expected;
}

struct Gen5StageResources
{
    array<device uint*, 1> buffers [[id(0)]];
    array<uint, 1> buffer_bytes [[id(1)]];
};

static inline uint sharpemu_load_word(device uint* b, uint bytes, uint addr)
{
    if ((addr & 3u) == 0u)
    {
        return addr + 4u <= bytes ? b[addr >> 2] : 0u;
    }
    uint value = 0u;
    device const uchar* p = (device const uchar*)b;
    for (uint i = 0u; i < 4u; i++)
    {
        if (addr + i < bytes)
        {
            value |= (uint)p[addr + i] << (i * 8u);
        }
    }
    return value;
}

static inline uint sharpemu_load_bytes(device uint* b, uint bytes, uint addr, uint count, bool signExtend)
{
    uint value = 0u;
    device const uchar* p = (device const uchar*)b;
    for (uint i = 0u; i < count; i++)
    {
        if (addr + i < bytes)
        {
            value |= (uint)p[addr + i] << (i * 8u);
        }
    }
    if (signExtend && count < 4u)
    {
        uint shift = 32u - (count * 8u);
        value = (uint)(((int)(value << shift)) >> shift);
    }
    return value;
}

static inline void sharpemu_store_bytes(device uint* b, uint bytes, uint addr, uint value, uint count)
{
    device uchar* p = (device uchar*)b;
    for (uint i = 0u; i < count; i++)
    {
        if (addr + i < bytes)
        {
            p[addr + i] = (uchar)((value >> (i * 8u)) & 0xFFu);
        }
    }
}

static inline void sharpemu_store_masked_word(device uint* b, uint bytes, uint index, uint value, uint mask)
{
    if (mask == 0u || index >= (bytes >> 2))
    {
        return;
    }
    if (mask == 0xFFFFFFFFu)
    {
        b[index] = value;
        return;
    }
    // The other bits of the word survive stores from neighbouring invocations.
    device atomic_uint* word = (device atomic_uint*)(b + index);
    uint observed = atomic_load_explicit(word, memory_order_relaxed);
    while (!atomic_compare_exchange_weak_explicit(word, &observed, (observed & ~mask) | value, memory_order_relaxed, memory_order_relaxed))
    {
    }
}

static inline void sharpemu_store_element(device uint* b, uint bytes, uint addr, uint count, uint4 values, uint4 masks)
{
    uint shift = (addr & 3u) * 8u;
    uint index = addr >> 2;
    uint carryValue = 0u;
    uint carryMask = 0u;
    for (uint k = 0u; k <= count; k++)
    {
        uint value = carryValue;
        uint mask = carryMask;
        if (k < count)
        {
            uint element = values[k] & masks[k];
            value |= element << shift;
            mask |= masks[k] << shift;
            carryValue = shift == 0u ? 0u : element >> (32u - shift);
            carryMask = shift == 0u ? 0u : masks[k] >> (32u - shift);
        }
        sharpemu_store_masked_word(b, bytes, index + k, value, mask);
    }
}

static inline uint sharpemu_ballot(bool value)
{
    return (uint)(uint64_t)simd_ballot(value);
}

static constant float sharpemu_off_i4_table[16] =
{
    0.0f, 0.0625f, 0.1250f, 0.1875f, 0.2500f, 0.3125f, 0.3750f, 0.4375f,
    -0.5000f, -0.4375f, -0.3750f, -0.3125f, -0.2500f, -0.1875f, -0.1250f, -0.0625f,
};

kernel void gen5_cs(
    constant Gen5StageResources& sharpemu_resources [[buffer(0)]],
    constant uint* sharpemu_push_data [[buffer(1)]],
    uint3 sharpemu_local_id [[thread_position_in_threadgroup]],
    uint3 sharpemu_group_id [[threadgroup_position_in_grid]],
    uint sharpemu_lane [[thread_index_in_simdgroup]])
{
    uint s[128] = {};
    uint v[256] = {};
    bool exec = true;
    bool vcc = false;
    bool scc = false;
    uint pc = 0u;
    bool active = true;
    uint steps = 0u;
    device uint* b0 = sharpemu_resources.buffers[0];
    uint bias[1] = {};
    s[8] = sharpemu_push_data[0];
    s[9] = sharpemu_push_data[1];
    s[10] = sharpemu_push_data[2];
    bias[0] = (sharpemu_push_data[3] >> 0u) & 0xFFu;
    v[0] = sharpemu_local_id.x;
    v[1] = sharpemu_local_id.y;
    v[2] = sharpemu_local_id.z;
    active = (sharpemu_group_id.x * 32u + sharpemu_local_id.x) < 0xFFFFFFFFu
        && (sharpemu_group_id.y * 1u + sharpemu_local_id.y) < 0xFFFFFFFFu
        && (sharpemu_group_id.z * 1u + sharpemu_local_id.z) < 0xFFFFFFFFu;
    s[106] = 0u;
    s[107] = 0u;
    s[126] = sharpemu_ballot(true);
    s[127] = 0u;

    while (active)
    {
        switch (pc)
        {
        case 0u:
        {
            uint t0 = 0x38FACu;
            s[11] = t0;
            uint t1 = ((0x0u + 0u + 0u + (0u * 0u)) + bias[0]);
            bool t2 = 8u <= sharpemu_resources.buffer_bytes[0] && t1 <= sharpemu_resources.buffer_bytes[0] - 8u;
            bool t3 = ((0xFACu >> 12) & 0x7Fu) != 0u && t2;
            uint t4 = 0u;
            uint t5 = 0u;
            uint t6 = 0u;
            sharpemu_format_layout(11u, 0u, t4, t5, t6);
            uint t7 = (t5 + t6 + 7u) >> 3u;
            bool t8 = t7 <= sharpemu_resources.buffer_bytes[0] && (t1 + t4) <= sharpemu_resources.buffer_bytes[0] - t7;
            bool t9 = t6 == 0u || t8;
            uint t10 = sharpemu_load_word(b0, sharpemu_resources.buffer_bytes[0], (t1 + t4));
            uint t11 = t6 == 0u ? 0u : extract_bits(t10, t5, t6);
            uint t12 = t6 == 0u ? 0u : sharpemu_format_convert(t11, t6, 7u, 11u);
            if (exec) { v[2] = t3 ? t12 : 0u; }
            uint t13 = 0u;
            uint t14 = 0u;
            uint t15 = 0u;
            sharpemu_format_layout(11u, 1u, t13, t14, t15);
            uint t16 = (t14 + t15 + 7u) >> 3u;
            bool t17 = t16 <= sharpemu_resources.buffer_bytes[0] && (t1 + t13) <= sharpemu_resources.buffer_bytes[0] - t16;
            bool t18 = t15 == 0u || t17;
            uint t19 = sharpemu_load_word(b0, sharpemu_resources.buffer_bytes[0], (t1 + t13));
            uint t20 = t15 == 0u ? 0u : extract_bits(t19, t14, t15);
            uint t21 = t15 == 0u ? 0u : sharpemu_format_convert(t20, t15, 7u, 11u);
            if (exec) { v[3] = t3 ? t21 : 0u; }
            uint t22 = ((0x10u + 0u + 0u + (0u * 0u)) + bias[0]);
            if (exec)
            {
                sharpemu_store_bytes(b0, sharpemu_resources.buffer_bytes[0], t22 + 0u, v[2], 4u);
            }
            uint t23 = ((0x14u + 0u + 0u + (0u * 0u)) + bias[0]);
            if (exec)
            {
                sharpemu_store_bytes(b0, sharpemu_resources.buffer_bytes[0], t23 + 0u, v[3], 4u);
            }
            active = false;
        }
        break;
        default:
            active = false;
            break;
        }
        if (++steps >= 100000u)
        {
            active = false;
        }
    }
}
